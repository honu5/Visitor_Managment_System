Visitor Management Frontend

Quick start:

1. cd frontend
2. npm install
3. npm run start

This project is a minimal scaffold that implements pages and API services matching the backend endpoints described in Architecture.md.
